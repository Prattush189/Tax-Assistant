node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# PM2 logs
logs/

# SQLite database (only the root-level data directory)
/data/
!server/data/
!server/data/*.txt
!src/data/

# transformers.js model cache (downloaded at runtime, ~30MB)
.models-cache/

# Bank payee labeling data (customer payee names — never commit)
payees-review*.json
payees-labeled*.json

# ad-hoc ledger samples (customer data)
shakti-sample.pdf
*-sample.pdf
