---
phase: 08-gst-act-data
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: []
autonomous: true
requirements: [GST-01, GST-02, GST-03, GST-04]

must_haves:
  truths:
    - "Server starts without errors and logs chunk counts for all 5 sources"
    - "CGST Act loads with > 150 chunks (quality gate)"
    - "IGST Act loads with > 20 chunks (quality gate)"
    - "GST query returns RAG-augmented answer citing CGST/IGST section numbers"
    - "GST chunk labels are visually distinguishable from IT Act chunk labels"
  artifacts: []
  key_links:
    - from: "server/rag/index.ts initRAG()"
      to: "server/data/cgst-act.txt"
      via: "SOURCE_CONFIGS filePath -> readFileSync -> splitActWithChaptersAndSchedules"
      pattern: "cgst-2017.*chunks"
    - from: "server/rag/index.ts initRAG()"
      to: "server/data/igst-act.txt"
      via: "SOURCE_CONFIGS filePath -> readFileSync -> splitActWithChaptersAndSchedules"
      pattern: "igst-2017.*chunks"
    - from: "server/routes/chat.ts"
      to: "server/rag/index.ts retrieveContext()"
      via: "RAG retrieval in chat route"
      pattern: "retrieveContext"
---

<objective>
Validate that GST Act data loads correctly and enables GST-specific RAG queries.

Purpose: Confirm the quality gates pass (CGST > 150 chunks, IGST > 20 chunks), GST chunks have proper labels, and end-to-end GST queries return Act-grounded answers.
Output: Validated GST RAG integration — server starts clean, chunk counts pass thresholds, and GST queries work.
</objective>

<execution_context>
@C:/Users/Prattush/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Prattush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gst-act-data/08-CONTEXT.md
@.planning/phases/08-gst-act-data/08-RESEARCH.md
@.planning/phases/08-gst-act-data/08-01-SUMMARY.md
@server/rag/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server startup validation and chunk count quality gates</name>
  <files></files>
  <action>
Start the server and capture startup logs to validate chunk counts for all 5 sources.

1. Run `npx tsx server/index.ts` (or the appropriate server start command) and capture the RAG initialization logs. The logs follow the pattern:
   ```
   [RAG] Loaded comparison: 98 chunks
   [RAG] Loaded act-2025: NNNN chunks
   [RAG] Loaded act-1961: NNNN chunks
   [RAG] Loaded cgst-2017: NNNN chunks
   [RAG] Loaded igst-2017: NN chunks
   [RAG] Total chunks: NNNNN, index keys: NNNNN
   ```

2. Validate quality gates:
   - cgst-2017 chunk count > 150 (MUST pass — if it fails, the CGST Act text format is wrong and Plan 01 Task 1 needs revision)
   - igst-2017 chunk count > 20 (MUST pass)
   - comparison, act-2025, act-1961 chunk counts should be unchanged from Phase 7 baseline (comparison: 98, act-2025: 2179, act-1961: 3636)

3. If CGST chunk count < 150:
   - Check `grep -cE "^[0-9]+[A-Z]*\. " server/data/cgst-act.txt` to verify section format
   - Check `grep -E "^CHAPTER [IVX]+" server/data/cgst-act.txt | wc -l` to verify chapter format
   - Fix formatting issues in cgst-act.txt until chunk count exceeds 150
   - Restart server and re-validate

4. If IGST chunk count < 20:
   - Same diagnostic steps as above for igst-act.txt
   - Fix and re-validate

5. Verify no startup errors (no uncaught exceptions, no file-not-found errors).

The server can be stopped after validation (Ctrl+C or kill process). This task is validation-only — if chunk counts pass, no code changes are needed.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && timeout 15 npx tsx server/index.ts 2>&1 | grep -E "\[RAG\]" || true</automated>
    <manual>Verify cgst-2017 > 150 chunks, igst-2017 > 20 chunks, and existing sources unchanged (comparison: 98, act-2025: ~2179, act-1961: ~3636)</manual>
  </verify>
  <done>Server starts without errors; cgst-2017 produces > 150 chunks; igst-2017 produces > 20 chunks; existing source chunk counts are unchanged</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end GST query validation</name>
  <files></files>
  <action>
With the server running, test GST-specific queries to verify RAG-augmented answers cite CGST/IGST sections correctly. This validates GST-04.

Run 3 test queries against the chat API (use curl or the running dev server):

**Query 1 (CGST — Input Tax Credit):**
```bash
curl -X POST http://localhost:4001/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "What is input tax credit under GST? Which sections govern ITC eligibility?", "history": []}'
```
Expected: Response mentions CGST Act Section 16 (eligibility) and/or Section 17 (blocked credits). The RAG context should include chunks labeled `[CGST Act 2017 — 16 [CHAPTER V...]]`.

**Query 2 (IGST — Place of Supply):**
```bash
curl -X POST http://localhost:4001/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "How is place of supply determined for services in GST?", "history": []}'
```
Expected: Response mentions IGST Act Section 12 (services within India) and/or Section 13 (international services).

**Query 3 (Mixed — GST vs IT):**
```bash
curl -X POST http://localhost:4001/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "What is the registration threshold under GST?", "history": []}'
```
Expected: Response mentions CGST Act Section 22 (registration requirement) with threshold amounts.

For each query:
- Confirm the response includes CGST or IGST section references (not just generic statements)
- Confirm the source labels in the response distinguish GST from IT Act content
- If the API streams responses (SSE), capture enough of the stream to see section references

Note: The chat endpoint may use SSE streaming. If so, use `curl --no-buffer` and capture output for a few seconds. The key verification is that GST Act sections appear in responses — exact phrasing varies by Gemini model output.

If responses do NOT cite GST sections despite correct chunk loading (Task 1 passed), this is likely a scoring issue for Phase 10 (SCOR-03), not a Phase 8 bug. Document the observation but do not modify retrieval scoring in this phase.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && timeout 30 npx tsx server/index.ts 2>&1 | grep -E "\[RAG\]" || true</automated>
    <manual>Send at least one GST query via curl to localhost:4001/api/chat and verify the response includes CGST/IGST section references</manual>
  </verify>
  <done>At least 2 of 3 test queries return responses that cite specific CGST or IGST section numbers; GST chunk labels in RAG output are distinguishable from IT Act labels</done>
</task>

</tasks>

<verification>
1. Server starts cleanly with 5 RAG sources loaded
2. CGST chunk count > 150 (quality gate GST-01)
3. IGST chunk count > 20 (quality gate GST-02)
4. Existing source chunk counts unchanged (no regression)
5. GST queries return Act-grounded answers with section citations (GST-04)
6. GST chunk labels include "CGST Act 2017" or "IGST Act 2017" prefix (GST-03)
</verification>

<success_criteria>
- Quality gates pass: CGST > 150 chunks, IGST > 20 chunks
- No regression: existing 3 sources load with same chunk counts as Phase 7
- GST queries produce RAG-augmented answers citing specific CGST/IGST sections
- Labels in retrieval output distinguish GST sources from IT Act sources
</success_criteria>

<output>
After completion, create `.planning/phases/08-gst-act-data/08-02-SUMMARY.md`
</output>
