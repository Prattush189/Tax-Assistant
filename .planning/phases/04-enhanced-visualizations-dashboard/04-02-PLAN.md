---
phase: 04-enhanced-visualizations-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat/ChartRenderer.tsx
  - server/routes/chat.ts
autonomous: true
requirements:
  - VIZ-02

must_haves:
  truths:
    - "ChartRenderer renders a line chart when type === 'line' with data array and optional 'lines' key list"
    - "ChartRenderer renders a stacked bar chart when type === 'stacked-bar' with data array and 'keys' key list"
    - "ChartRenderer renders a composed chart (bars + lines) when type === 'composed' with 'bars' and 'lines' key lists"
    - "Existing 'bar' and 'pie' chart types continue to work unchanged"
    - "The AI system prompt documents new chart types so the model knows to emit them"
  artifacts:
    - path: "src/components/chat/ChartRenderer.tsx"
      provides: "Extended chart renderer with line, stacked-bar, composed types"
      contains: "LineChart"
    - path: "server/routes/chat.ts"
      provides: "Updated SYSTEM_INSTRUCTION with new chart type documentation"
      contains: "stacked-bar"
  key_links:
    - from: "server/routes/chat.ts"
      to: "src/components/chat/ChartRenderer.tsx"
      via: "AI emits json-chart block; client parses and routes to correct chart component"
      pattern: "stacked-bar|composed|line"
---

<objective>
Extend ChartRenderer.tsx with three new chart types (line, stacked-bar, composed) and update the AI system prompt to document them.

Purpose: Currently the AI can only emit 'bar' or 'pie' charts. Line charts are essential for showing tax rate progressions over income levels; stacked-bar for deduction breakdowns; composed for overlaying tax + income on one chart. These are the chart types the AI needs to produce richer analytical answers (VIZ-02).

Output:
- `src/components/chat/ChartRenderer.tsx` — three new switch cases added, existing cases untouched
- `server/routes/chat.ts` — SYSTEM_INSTRUCTION updated to document new types and their JSON schemas
</objective>

<execution_context>
@C:/Users/Prattush/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Prattush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-enhanced-visualizations-dashboard/04-RESEARCH.md
@src/components/chat/ChartRenderer.tsx
@server/routes/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ChartRenderer with line, stacked-bar, composed</name>
  <files>src/components/chat/ChartRenderer.tsx</files>
  <action>
    Extend `ChartRenderer.tsx`. The existing BarChart and PieChart cases must remain exactly as-is (same JSX, same import list). Add new imports and new switch cases.

    **New imports to add** (add to the existing recharts import line):
    `LineChart, Line, ComposedChart, Area` — note: `BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell, Legend` are already imported; add only what is missing.
    Import list to use: `BarChart, Bar, LineChart, Line, ComposedChart, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell, Legend`

    **Change the render logic** from the current `{type === 'bar' ? <BarChart...> : <PieChart...>}` ternary to a `renderChart()` function or inline switch. Use a switch on `type`:

    ```
    case 'line':
      <LineChart data={data}>
        <CartesianGrid strokeDasharray="3 3" stroke="#94a3b8" opacity={0.2} />
        <XAxis dataKey="name" fontSize={12} stroke="#94a3b8" />
        <YAxis fontSize={12} stroke="#94a3b8" />
        <Tooltip contentStyle={{ backgroundColor: '#1e293b', border: 'none', borderRadius: '8px', color: '#fff' }} itemStyle={{ color: '#fff' }} />
        <Legend />
        {(chartData.lines ?? ['value']).map((key: string, i: number) => (
          <Line key={key} type="monotone" dataKey={key} stroke={COLORS[i % COLORS.length]} strokeWidth={2} dot={false} />
        ))}
      </LineChart>

    case 'stacked-bar':
      <BarChart data={data}>
        <CartesianGrid strokeDasharray="3 3" stroke="#94a3b8" opacity={0.2} />
        <XAxis dataKey="name" fontSize={12} stroke="#94a3b8" />
        <YAxis fontSize={12} stroke="#94a3b8" />
        <Tooltip contentStyle={{ backgroundColor: '#1e293b', border: 'none', borderRadius: '8px', color: '#fff' }} itemStyle={{ color: '#fff' }} />
        <Legend />
        {(chartData.keys ?? ['value']).map((key: string, i: number) => (
          <Bar key={key} dataKey={key} stackId="a" fill={COLORS[i % COLORS.length]} />
        ))}
      </BarChart>

    case 'composed':
      <ComposedChart data={data}>
        <CartesianGrid strokeDasharray="3 3" stroke="#94a3b8" opacity={0.2} />
        <XAxis dataKey="name" fontSize={12} stroke="#94a3b8" />
        <YAxis fontSize={12} stroke="#94a3b8" />
        <Tooltip contentStyle={{ backgroundColor: '#1e293b', border: 'none', borderRadius: '8px', color: '#fff' }} itemStyle={{ color: '#fff' }} />
        <Legend />
        {(chartData.bars ?? []).map((key: string, i: number) => (
          <Bar key={key} dataKey={key} fill={COLORS[i % COLORS.length]} radius={[4, 4, 0, 0]} />
        ))}
        {(chartData.lines ?? []).map((key: string, i: number) => (
          <Line key={key} type="monotone" dataKey={key} stroke={COLORS[(i + 3) % COLORS.length]} strokeWidth={2} dot={false} />
        ))}
      </ComposedChart>
    ```

    For the default case (unknown type), render `null` — consistent with how the try/catch already returns null on parse failure.

    **Critical recharts 3.x rules:**
    - Do NOT use `CategoricalChartState`, `activeIndex` prop on Bar, or `TooltipProps`.
    - The `contentStyle` / `itemStyle` pattern on Tooltip is correct v3 API — use it for all new chart types to keep visual consistency with existing charts.
    - Wrap the entire switch result in `<ResponsiveContainer width="100%" height="100%">` as is already done.

    **TypeScript:** `chartData` is already `JSON.parse(jsonString)` with type `any` — accessing `.lines`, `.keys`, `.bars` is fine without additional type assertions. Keep the existing `try/catch` returning `null` on parse failure.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>In browser, open Chat tab and send: "Show me a line chart of effective tax rate at 5L, 10L, 15L, 20L income levels" — confirm AI renders a line chart (not bar/pie).</manual>
  </verify>
  <done>TypeScript compiles clean. ChartRenderer handles line, stacked-bar, composed types. Existing bar and pie types unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update AI system prompt with new chart type documentation</name>
  <files>server/routes/chat.ts</files>
  <action>
    In `server/routes/chat.ts`, update the `SYSTEM_INSTRUCTION` constant's CHART GENERATION section. The current text only documents `"bar" | "pie"` types.

    Replace the chart generation block (the paragraph starting "CHART GENERATION:") with this updated version:

    ```
    CHART GENERATION:
    When providing statistical data or tax comparisons, you MUST also provide a JSON block for visualization.
    Format: \`\`\`json-chart { ... } \`\`\`

    Supported chart types:

    "bar" — single-series bar chart
    { "type": "bar", "title": "...", "data": [{ "name": "...", "value": 123 }, ...] }

    "pie" — donut/pie chart
    { "type": "pie", "title": "...", "data": [{ "name": "...", "value": 123 }, ...] }

    "line" — line chart (use for trends over income/time)
    { "type": "line", "title": "...", "data": [{ "name": "5L", "rate": 0 }, ...], "lines": ["rate"] }
    "lines" is required — list the data keys to plot as lines.

    "stacked-bar" — stacked bar chart (use for showing composition/breakdown)
    { "type": "stacked-bar", "title": "...", "data": [{ "name": "80C", "used": 150000, "remaining": 50000 }], "keys": ["used", "remaining"] }
    "keys" is required — list the data keys to stack.

    "composed" — combined bar + line chart (use for overlaying two different scales)
    { "type": "composed", "title": "...", "data": [{ "name": "FY24", "income": 1200000, "tax": 97500 }], "bars": ["income"], "lines": ["tax"] }
    "bars" and "lines" are required — list keys for bar series and line series respectively.

    Use "line" for: effective tax rate progression, year-over-year comparison.
    Use "stacked-bar" for: deduction breakdown (used vs. remaining), income composition.
    Use "composed" for: income vs. tax overlay, gross vs. net comparison.
    ```

    Do not change any other part of SYSTEM_INSTRUCTION (the expertise list, table formatting rules, guidelines, or disclaimer).
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm server/routes/chat.ts contains "stacked-bar" and "composed" in SYSTEM_INSTRUCTION.</manual>
  </verify>
  <done>SYSTEM_INSTRUCTION documents all 5 chart types with JSON schemas and usage guidance. TypeScript clean.</done>
</task>

</tasks>

<verification>
Run: `cd D:/tax-assistant && npx tsc --noEmit`
Expected: Zero TypeScript errors.
Check: `grep -c "stacked-bar" D:/tax-assistant/server/routes/chat.ts` outputs >= 1
Check: `grep -c "LineChart" D:/tax-assistant/src/components/chat/ChartRenderer.tsx` outputs >= 1
</verification>

<success_criteria>
- ChartRenderer.tsx handles 5 chart types: bar, pie, line, stacked-bar, composed
- line/stacked-bar/composed use the same dark tooltip style as existing bar/pie
- SYSTEM_INSTRUCTION documents all 5 types with correct JSON schemas
- No new npm packages installed
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhanced-visualizations-dashboard/04-02-SUMMARY.md`
</output>
