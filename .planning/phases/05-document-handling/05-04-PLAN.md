---
phase: 05-document-handling
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/components/documents/DocumentsView.tsx
  - src/components/documents/DocumentCard.tsx
  - src/components/chat/ChatInput.tsx
  - src/components/layout/Header.tsx
  - src/App.tsx
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "A Documents tab appears in Header navigation; clicking it renders DocumentsView"
    - "DocumentsView shows a drag-and-drop upload zone accepting PDF and image files"
    - "After upload, DocumentCard renders extracted summary: document type, FY, employer, employee, salary, TDS, deductions"
    - "DocumentCard shows a Dismiss button that detaches the document and resets upload state"
    - "When a document is active, ChatInput shows a badge like 'Form 16 attached' above the input area"
    - "Upload errors show inline below the upload zone as red text — not as toasts or chat bubbles"
    - "During upload, two-phase UX states are shown: 'Uploading document...' then 'Analyzing with AI...'"
    - "Switching to Chat tab with an active document shows the document badge in ChatInput"
  artifacts:
    - path: "src/components/documents/DocumentsView.tsx"
      provides: "Upload zone with drag-and-drop, two-phase upload UX, inline error, DocumentCard render"
      min_lines: 80
    - path: "src/components/documents/DocumentCard.tsx"
      provides: "Extracted summary display with all DocumentSummary fields and Dismiss action"
      min_lines: 60
    - path: "src/components/chat/ChatInput.tsx"
      provides: "Document attached badge shown when activeDocument is set"
    - path: "src/components/layout/Header.tsx"
      provides: "Documents tab added to navigation"
    - path: "src/App.tsx"
      provides: "ActiveView includes 'documents'; DocumentsView rendered; attachDocument wired from useChat to DocumentsView"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/documents/DocumentsView.tsx"
      via: "onDocumentAttach prop passing useChat().attachDocument"
      pattern: "attachDocument"
    - from: "src/components/documents/DocumentsView.tsx"
      to: "src/services/api.ts uploadFile"
      via: "uploadFile(file) call on form submit / drop"
      pattern: "uploadFile"
    - from: "src/components/chat/ChatInput.tsx"
      to: "useChat activeDocument"
      via: "activeDocument prop showing badge when non-null"
      pattern: "activeDocument"
---

<objective>
Build the DocumentsView tab with upload zone, DocumentCard summary display, and wire document context into ChatInput's badge — completing the end-to-end document handling flow.

Purpose: This plan delivers the user-facing side of Phase 5. Users can upload a document, see the extracted summary, then switch to Chat and see their document is active. All four DOC requirements become observable from the UI.
Output: DocumentsView.tsx, DocumentCard.tsx (new files), extended ChatInput.tsx, Header.tsx, and App.tsx.
</objective>

<execution_context>
@C:/Users/Prattush/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Prattush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-handling/05-RESEARCH.md
@.planning/phases/05-document-handling/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DocumentCard.tsx and DocumentsView.tsx</name>
  <files>
    src/components/documents/DocumentCard.tsx
    src/components/documents/DocumentsView.tsx
  </files>
  <action>
Create two new files in src/components/documents/.

**DocumentCard.tsx** — displays the extracted summary from a DocumentContext:

```typescript
import { X, FileText } from 'lucide-react';
import { DocumentContext } from '../../types';
import { cn } from '../../lib/utils';

function formatINR(value: number | null): string {
  if (value === null) return '—';
  return new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(value);
}

interface DocumentCardProps {
  document: DocumentContext;
  onDismiss: () => void;
}

export function DocumentCard({ document, onDismiss }: DocumentCardProps) {
  const d = document.extractedData;

  return (
    <div className="rounded-xl border border-green-200 dark:border-green-800 bg-green-50 dark:bg-green-950/30 p-4 space-y-3">
      <div className="flex items-start justify-between gap-2">
        <div className="flex items-center gap-2">
          <FileText className="w-5 h-5 text-green-600 dark:text-green-400 shrink-0" />
          <div>
            <p className="font-semibold text-slate-800 dark:text-slate-100 text-sm">{document.filename}</p>
            {d.documentType && (
              <p className="text-xs text-green-700 dark:text-green-400">{d.documentType}{d.financialYear ? ` · FY ${d.financialYear}` : ''}</p>
            )}
          </div>
        </div>
        <button
          onClick={onDismiss}
          aria-label="Dismiss document"
          className="p-1 rounded hover:bg-green-100 dark:hover:bg-green-900/50 text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-200 shrink-0"
        >
          <X className="w-4 h-4" />
        </button>
      </div>

      {d.summary && (
        <p className="text-sm text-slate-600 dark:text-slate-400 italic">{d.summary}</p>
      )}

      <div className="grid grid-cols-2 gap-x-4 gap-y-1.5 text-sm">
        {d.employerName && (
          <><span className="text-slate-500 dark:text-slate-400">Employer</span><span className="text-slate-800 dark:text-slate-100 font-medium">{d.employerName}</span></>
        )}
        {d.employeeName && (
          <><span className="text-slate-500 dark:text-slate-400">Employee</span><span className="text-slate-800 dark:text-slate-100 font-medium">{d.employeeName}</span></>
        )}
        {d.pan && (
          <><span className="text-slate-500 dark:text-slate-400">PAN</span><span className="text-slate-800 dark:text-slate-100 font-medium font-mono">{d.pan}</span></>
        )}
        {d.grossSalary !== null && (
          <><span className="text-slate-500 dark:text-slate-400">Gross Salary</span><span className="text-slate-800 dark:text-slate-100 font-medium">{formatINR(d.grossSalary)}</span></>
        )}
        {d.taxableSalary !== null && (
          <><span className="text-slate-500 dark:text-slate-400">Taxable Salary</span><span className="text-slate-800 dark:text-slate-100 font-medium">{formatINR(d.taxableSalary)}</span></>
        )}
        {d.tdsDeducted !== null && (
          <><span className="text-slate-500 dark:text-slate-400">TDS Deducted</span><span className="text-emerald-700 dark:text-emerald-400 font-medium">{formatINR(d.tdsDeducted)}</span></>
        )}
        {d.deductions80C !== null && (
          <><span className="text-slate-500 dark:text-slate-400">80C</span><span className="text-slate-800 dark:text-slate-100 font-medium">{formatINR(d.deductions80C)}</span></>
        )}
        {d.deductions80D !== null && (
          <><span className="text-slate-500 dark:text-slate-400">80D</span><span className="text-slate-800 dark:text-slate-100 font-medium">{formatINR(d.deductions80D)}</span></>
        )}
      </div>

      <p className="text-xs text-slate-400 dark:text-slate-500">Document active — ask questions about it in the Chat tab.</p>
    </div>
  );
}
```

**DocumentsView.tsx** — upload zone with two-phase UX and inline error display:

```typescript
import { useRef, useState } from 'react';
import { Upload, FileText } from 'lucide-react';
import { uploadFile } from '../../services/api';
import { DocumentContext } from '../../types';
import { DocumentCard } from './DocumentCard';
import { cn } from '../../lib/utils';

type UploadPhase = 'idle' | 'uploading' | 'analyzing' | 'done' | 'error';

interface DocumentsViewProps {
  activeDocument: DocumentContext | null;
  onDocumentAttach: (doc: DocumentContext) => void;
  onDocumentDetach: () => void;
}

export function DocumentsView({ activeDocument, onDocumentAttach, onDocumentDetach }: DocumentsViewProps) {
  const [uploadPhase, setUploadPhase] = useState<UploadPhase>('idle');
  const [errorMessage, setErrorMessage] = useState<string>('');
  const [isDraggingOver, setIsDraggingOver] = useState(false);
  const inputRef = useRef<HTMLInputElement>(null);

  const handleFile = async (file: File) => {
    setErrorMessage('');
    setUploadPhase('uploading');

    try {
      // Phase 1: uploading (multer + Files API transfer)
      // Small delay visual: the fetch itself provides the uploading state
      // Phase 2: analyzing (generateContent call, typically longer)
      // We transition to 'analyzing' after a short buffer — the server does both steps
      // in one request, so we use a timeout heuristic for the two-phase UX display
      const analyzeTimer = setTimeout(() => {
        setUploadPhase('analyzing');
      }, 1500);

      const result = await uploadFile(file);
      clearTimeout(analyzeTimer);

      const doc: DocumentContext = {
        filename: result.filename,
        mimeType: result.mimeType,
        fileUri: result.fileUri,
        extractedData: result.extractedData,
      };

      onDocumentAttach(doc);
      setUploadPhase('done');
    } catch (err) {
      setUploadPhase('error');
      setErrorMessage(err instanceof Error ? err.message : 'Upload failed. Please try again.');
    }
  };

  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (file) handleFile(file);
    // Reset input so same file can be re-selected after dismissal
    e.target.value = '';
  };

  const handleDrop = (e: React.DragEvent<HTMLDivElement>) => {
    e.preventDefault();
    setIsDraggingOver(false);
    const file = e.dataTransfer.files?.[0];
    if (file) handleFile(file);
  };

  const handleDismiss = () => {
    onDocumentDetach();
    setUploadPhase('idle');
    setErrorMessage('');
  };

  const isProcessing = uploadPhase === 'uploading' || uploadPhase === 'analyzing';

  return (
    <div className="flex-1 overflow-y-auto p-4 lg:p-8 max-w-2xl mx-auto w-full">
      <div className="space-y-6">
        <div>
          <h2 className="text-xl font-semibold text-slate-800 dark:text-slate-100">Documents</h2>
          <p className="text-sm text-slate-500 dark:text-slate-400 mt-1">
            Upload Form 16, salary slips, or investment proofs for AI analysis. Ask follow-up questions in the Chat tab.
          </p>
        </div>

        {/* Upload zone — hidden when a document is already active */}
        {!activeDocument && (
          <div
            role="button"
            tabIndex={0}
            aria-label="Upload tax document"
            onClick={() => !isProcessing && inputRef.current?.click()}
            onKeyDown={(e) => e.key === 'Enter' && !isProcessing && inputRef.current?.click()}
            onDragOver={(e) => { e.preventDefault(); setIsDraggingOver(true); }}
            onDragLeave={() => setIsDraggingOver(false)}
            onDrop={handleDrop}
            className={cn(
              "border-2 border-dashed rounded-xl p-8 text-center transition-colors cursor-pointer",
              isDraggingOver
                ? "border-orange-400 bg-orange-50 dark:bg-orange-950/20"
                : "border-slate-300 dark:border-slate-600 hover:border-orange-400 hover:bg-slate-50 dark:hover:bg-slate-800/50",
              isProcessing && "cursor-not-allowed opacity-70"
            )}
          >
            <input
              ref={inputRef}
              type="file"
              accept=".pdf,image/jpeg,image/png,image/webp,image/heic"
              className="hidden"
              onChange={handleInputChange}
              disabled={isProcessing}
            />

            {isProcessing ? (
              <div className="space-y-2">
                <div className="w-8 h-8 border-2 border-orange-400 border-t-transparent rounded-full animate-spin mx-auto" />
                <p className="text-sm font-medium text-slate-600 dark:text-slate-300">
                  {uploadPhase === 'uploading' ? 'Uploading document...' : 'Analyzing with AI...'}
                </p>
                <p className="text-xs text-slate-400 dark:text-slate-500">This may take a few seconds</p>
              </div>
            ) : (
              <div className="space-y-3">
                <div className="flex justify-center">
                  <div className="w-12 h-12 rounded-full bg-orange-100 dark:bg-orange-900/30 flex items-center justify-center">
                    <Upload className="w-6 h-6 text-orange-500" />
                  </div>
                </div>
                <div>
                  <p className="font-medium text-slate-700 dark:text-slate-200">Drop your document here</p>
                  <p className="text-sm text-slate-500 dark:text-slate-400 mt-1">or click to browse</p>
                </div>
                <div className="flex items-center justify-center gap-2 flex-wrap">
                  {['PDF', 'JPEG', 'PNG', 'WebP', 'HEIC'].map(fmt => (
                    <span key={fmt} className="inline-flex items-center gap-1 text-xs px-2 py-0.5 rounded bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400">
                      <FileText className="w-3 h-3" />{fmt}
                    </span>
                  ))}
                </div>
                <p className="text-xs text-slate-400 dark:text-slate-500">Maximum 10MB</p>
              </div>
            )}
          </div>
        )}

        {/* Inline error — shown below upload zone, not as a toast */}
        {uploadPhase === 'error' && errorMessage && (
          <p className="text-sm text-red-600 dark:text-red-400">{errorMessage}</p>
        )}

        {/* Document summary card */}
        {activeDocument && (
          <DocumentCard document={activeDocument} onDismiss={handleDismiss} />
        )}
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm DocumentsView.tsx and DocumentCard.tsx exist in src/components/documents/. Check that the upload zone shows "Uploading document..." / "Analyzing with AI..." phases and that errors render inline as red text (not toast).</manual>
  </verify>
  <done>Both files created without TypeScript errors. DocumentCard renders all DocumentSummary fields with INR formatting. DocumentsView shows two-phase upload UX and inline error display.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx, Header.tsx, and ChatInput.tsx for Documents tab</name>
  <files>
    src/App.tsx
    src/components/layout/Header.tsx
    src/components/chat/ChatInput.tsx
  </files>
  <action>
Make targeted changes to connect the Documents tab through the application.

**src/App.tsx:**

1. Add DocumentsView import:
   `import { DocumentsView } from './components/documents/DocumentsView';`

2. Change the `ActiveView` type:
   Change: `type ActiveView = 'chat' | 'calculator' | 'dashboard';`
   To: `type ActiveView = 'chat' | 'calculator' | 'dashboard' | 'documents';`

3. Add useChat import and call in App.tsx to expose attachDocument/detachDocument/activeDocument.
   Add import: `import { useChat } from './hooks/useChat';`
   Inside App(), after existing useState calls, add:
   ```typescript
   const { activeDocument, attachDocument, detachDocument } = useChat();
   ```

4. Add DocumentsView to the rendered views, after the existing `{activeView === 'dashboard' && <DashboardView />}` line:
   ```typescript
   {activeView === 'documents' && (
     <DocumentsView
       activeDocument={activeDocument}
       onDocumentAttach={attachDocument}
       onDocumentDetach={detachDocument}
     />
   )}
   ```

5. Pass activeDocument down to ChatView so it can show the badge in ChatInput:
   The existing `<ChatView isPluginMode={isPluginMode} />` becomes:
   ```typescript
   <ChatView isPluginMode={isPluginMode} activeDocument={activeDocument} />
   ```

**IMPORTANT NOTE on useChat in App.tsx:** App.tsx will now call useChat() for document context. ChatView internally calls useChat() for its own chat state. These are TWO separate hook instances. This means the document context in App.tsx and the document-aware sendChatMessage in ChatView's useChat are different instances. To fix this: ChatView must receive activeDocument as a prop and pass it through to its internal sendChatMessage calls — OR we need to lift the entire useChat call to App.tsx and pass everything down as props.

**Chosen approach (simpler, avoids prop drilling):** Pass activeDocument as a prop to ChatView. ChatView passes it to ChatInput for the badge. For the actual send, ChatView's internal useChat already has attachDocument — but the App-level document attachment comes from DocumentsView calling the App-level attachDocument. Since these are two different useChat instances, the document context won't flow automatically.

**Correct fix:** Lift useChat entirely to App.tsx. Pass all needed values as props to ChatView and DocumentsView. This matches how TaxCalculatorProvider lifted state for DashboardView.

Revise App.tsx to lift useChat:
```typescript
import { useChat } from './hooks/useChat';
// In App():
const chatHook = useChat();  // single instance
```

Then pass to ChatView:
```typescript
<ChatView isPluginMode={isPluginMode} chatHook={chatHook} />
```

And to DocumentsView:
```typescript
<DocumentsView
  activeDocument={chatHook.activeDocument}
  onDocumentAttach={chatHook.attachDocument}
  onDocumentDetach={chatHook.detachDocument}
/>
```

**src/components/chat/ChatView.tsx** — this requires a companion update:
ChatView currently calls `useChat()` internally. It must be updated to accept chatHook as a prop OR to accept individual props. The cleanest approach: accept a `chatHook` prop of type `ReturnType<typeof useChat>`.

Update ChatView.tsx:
- Import `useChat` type: `import { useChat } from '../../hooks/useChat';`
- Add prop: `chatHook: ReturnType<typeof useChat>`
- Remove the internal `const { messages, ... } = useChat();` call
- Use `chatHook.messages`, `chatHook.input`, etc. instead

Also update ChatInput.tsx to accept and display activeDocument badge.

**src/components/chat/ChatInput.tsx:**
Read the current file to understand its props interface, then add:
- `activeDocument?: { filename: string } | null` to the props interface
- Above the textarea, render when activeDocument is set:
  ```typescript
  {activeDocument && (
    <div className="px-3 py-1.5 bg-green-50 dark:bg-green-950/30 border-b border-green-200 dark:border-green-800 flex items-center gap-2 text-xs text-green-700 dark:text-green-400">
      <FileText className="w-3 h-3 shrink-0" />
      <span>{activeDocument.filename} attached — answers will reference this document</span>
    </div>
  )}
  ```
  Import FileText from lucide-react.

**src/components/layout/Header.tsx:**
1. Change the `ActiveView` type to include 'documents':
   Change: `type ActiveView = 'chat' | 'calculator' | 'dashboard';`
   To: `type ActiveView = 'chat' | 'calculator' | 'dashboard' | 'documents';`

2. Add 'documents' tab to the tabs array:
   Change:
   ```typescript
   const tabs: { id: ActiveView; label: string }[] = [
     { id: 'chat', label: 'Chat' },
     { id: 'calculator', label: 'Calculator' },
     { id: 'dashboard', label: 'Dashboard' },
   ];
   ```
   To:
   ```typescript
   const tabs: { id: ActiveView; label: string }[] = [
     { id: 'chat', label: 'Chat' },
     { id: 'calculator', label: 'Calculator' },
     { id: 'dashboard', label: 'Dashboard' },
     { id: 'documents', label: 'Documents' },
   ];
   ```

Before making changes, READ each file first to preserve existing code accurately.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>
      1. Start dev server: npm run dev in D:/tax-assistant
      2. Open http://localhost:5173 — verify "Documents" tab appears in Header
      3. Click Documents tab — verify upload zone renders
      4. Chat tab should show no badge initially
    </manual>
  </verify>
  <done>TypeScript compile passes. Header has 4 tabs. DocumentsView renders at documents route. ChatView accepts chatHook prop and renders chat correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — end-to-end document upload, extraction, and document-aware chat</name>
  <action>Human verifies the complete document handling flow end-to-end as described in how-to-verify steps below.</action>
  <verify><manual>Follow the 6-step verification checklist in how-to-verify.</manual></verify>
  <done>All 6 verification steps pass: Documents tab renders, upload extracts summary, URI not in localStorage, chat badge shows, document-aware Q  <what-built>A works, dismiss resets state, error renders inline.</done>
  <what-built>
    Complete Phase 5 document handling:
    - server/routes/upload.ts: Gemini Files API pipeline (Buffer→Blob→upload→extract→response)
    - server/routes/chat.ts: optional fileContext injection for document-aware Q&A
    - src/types/index.ts: DocumentSummary, DocumentContext, updated UploadResponse
    - src/services/api.ts: fileContext param in sendChatMessage
    - src/hooks/useChat.ts: activeDocument state with attach/detach
    - src/components/documents/DocumentsView.tsx + DocumentCard.tsx: upload zone + summary card
    - src/App.tsx + Header.tsx + ChatInput.tsx: Documents tab wiring
  </what-built>
  <how-to-verify>
    Run: npm run dev in D:/tax-assistant, then open http://localhost:5173

    1. **Documents tab visible** — Header shows "Chat | Calculator | Dashboard | Documents"

    2. **DOC-01 + DOC-03: Upload and extract summary**
       - Click "Documents" tab
       - Upload a PDF (any PDF — salary slip, or any multi-page document)
       - Verify: "Uploading document..." then "Analyzing with AI..." spinner states appear
       - Verify: DocumentCard renders with filename, document type, and at least the summary sentence
       - Verify: No errors shown

    3. **DOC-04: Session-only URI**
       - Open browser DevTools → Application → Local Storage and Session Storage
       - Verify: No `fileUri` key anywhere in storage — only React state holds it

    4. **DOC-02: Document-aware chat**
       - With DocumentCard visible, switch to Chat tab
       - Verify: ChatInput shows green badge "YourFile.pdf attached..."
       - Ask: "What document have I uploaded?"
       - Verify: AI response references the document content (not generic)

    5. **Dismiss + reset**
       - In Documents tab, click X on DocumentCard
       - Verify: Upload zone reappears
       - Switch to Chat — verify badge is gone

    6. **Error handling**
       - Try uploading a .txt file — verify inline red error: "Invalid file type..."
       - (No toast, no chat bubble — red text below upload zone)
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 steps pass, or describe which steps failed with error details</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors.
2. `npm run dev` starts without errors.
3. All 6 verification steps in the checkpoint pass.
4. No fileUri found in localStorage or sessionStorage during document session.
5. Chat responses reference document content when fileContext is active.
</verification>

<success_criteria>
- Documents tab renders DocumentsView with drag-and-drop upload zone
- Upload shows two-phase "Uploading..." / "Analyzing..." states
- DocumentCard shows extracted Form 16 / salary slip fields
- Chat badge shows filename when document is active
- Document-aware chat answers reference uploaded document content
- Upload errors are inline red text, not toasts
- TypeScript compile clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-handling/05-04-SUMMARY.md`
</output>
