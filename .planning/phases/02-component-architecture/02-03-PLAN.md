---
phase: 02-component-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/components/chat/ChartRenderer.tsx
  - src/components/chat/ChatInput.tsx
  - src/components/chat/MessageBubble.tsx
  - src/components/chat/ChatView.tsx
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "A JSON chart block in a model message renders as a bar or pie chart with title"
    - "User can type in the chat input and press Enter or the send button to trigger send"
    - "Each message displays as a styled bubble with rendered markdown and inline charts"
    - "The chat view shows the message list, empty state when no messages, loading indicator, and input area"
  artifacts:
    - path: "src/components/chat/ChartRenderer.tsx"
      provides: "Bar and pie chart rendering from json-chart code blocks"
      exports: ["ChartRenderer"]
    - path: "src/components/chat/ChatInput.tsx"
      provides: "Textarea + send button UI"
      exports: ["ChatInput"]
    - path: "src/components/chat/MessageBubble.tsx"
      provides: "Single message rendering with renderContent splitting markdown from charts"
      exports: ["MessageBubble"]
    - path: "src/components/chat/ChatView.tsx"
      provides: "Full chat UI — calls useChat, composes MessageBubble list + ChatInput"
      exports: ["ChatView"]
  key_links:
    - from: "src/components/chat/ChatView.tsx"
      to: "src/hooks/useChat.ts"
      via: "useChat() call inside ChatView"
      pattern: "useChat\\(\\)"
    - from: "src/components/chat/MessageBubble.tsx"
      to: "src/components/chat/ChartRenderer.tsx"
      via: "renderContent splits on json-chart, renders ChartRenderer"
      pattern: "ChartRenderer"
---

<objective>
Extract the four chat-specific UI components from App.tsx: ChartRenderer (charts from AI JSON blocks), ChatInput (textarea + send), MessageBubble (single message with content rendering), and ChatView (full chat layout that owns useChat state).

Purpose: These components represent the complete visible chat interface. ChatView calls useChat() internally — it does not receive chat state as props, keeping App.tsx free of chat concerns.
Output: src/components/chat/ directory with four named-export components
</objective>

<execution_context>
@C:/Users/Prattush/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Prattush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-architecture/02-RESEARCH.md
@src/App.tsx
@.planning/phases/02-component-architecture/02-01-SUMMARY.md
@.planning/phases/02-component-architecture/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChartRenderer, ChatInput, and MessageBubble components</name>
  <files>
    src/components/chat/ChartRenderer.tsx,
    src/components/chat/ChatInput.tsx,
    src/components/chat/MessageBubble.tsx
  </files>
  <action>
**ChartRenderer (src/components/chat/ChartRenderer.tsx):**
Extract verbatim from App.tsx lines 56-106. Named export `ChartRenderer`. Props: `{ jsonString: string }`. Keep the COLORS constant local to this file. Import from 'recharts' and keep all existing chart config (CartesianGrid strokeDasharray, Tooltip contentStyle dark background, innerRadius/outerRadius on Pie, radius on Bar) unchanged. Keep the try/catch returning null on parse failure. Do NOT import cn() — no conditional classes needed here.

Keep the import as `from 'motion/react'` throughout all components (NOT 'framer-motion').

**ChatInput (src/components/chat/ChatInput.tsx):**
Extract the `<textarea>` + send button from App.tsx (lines 503-534 area). Named export `ChatInput`. Props interface:
```typescript
interface ChatInputProps {
  input: string;
  isLoading: boolean;
  onInputChange: (value: string) => void;
  onSend: () => void;
}
```
- Textarea: value={input}, onChange calls onInputChange(e.target.value), onKeyDown calls onSend() on Enter without Shift key (match App.tsx behavior exactly — `if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); onSend(); }`)
- Send button: onClick calls onSend(), disabled when isLoading or input.trim() === ''
- Import cn from '../../lib/utils', import Send icon from 'lucide-react'
- Preserve all Tailwind classes from App.tsx for the input area container, textarea, and button

**MessageBubble (src/components/chat/MessageBubble.tsx):**
Extract the individual message rendering from App.tsx. Named export `MessageBubble`. Props interface:
```typescript
interface MessageBubbleProps {
  message: Message;
}
```
Include the `renderContent` helper function (NOT exported — local to this file):
```typescript
function renderContent(content: string, role: 'user' | 'model') {
  const parts = content.split(/```json-chart([\s\S]*?)```/);
  return parts.map((part, index) => {
    if (index % 2 === 1) {
      return <ChartRenderer key={index} jsonString={part.trim()} />;
    }
    return (
      <div key={index} className={cn(
        "markdown-body prose max-w-none prose-sm sm:prose-base overflow-x-auto",
        role === 'user' ? 'prose-invert' : 'prose-slate dark:prose-invert'
      )}>
        <Markdown remarkPlugins={[remarkGfm]}>{part}</Markdown>
      </div>
    );
  });
}
```
The MessageBubble component renders the role avatar (Bot/User icon from lucide-react), the bubble container with dark/light mode Tailwind classes from App.tsx, and calls renderContent. Import Message from '../../types', cn from '../../lib/utils', ChartRenderer from './ChartRenderer', Markdown from 'react-markdown', remarkGfm from 'remark-gfm'.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirm ChartRenderer, ChatInput, MessageBubble all have named exports. Confirm renderContent is NOT exported from MessageBubble. Run: grep -n "from 'framer-motion'" src/components/chat/*.tsx — should return zero results.</manual>
  </verify>
  <done>Three components compile with zero TypeScript errors. ChartRenderer handles bar and pie types. ChatInput fires onSend on Enter-without-Shift. MessageBubble uses renderContent to split json-chart blocks from markdown.</done>
</task>

<task type="auto">
  <name>Task 2: ChatView component</name>
  <files>src/components/chat/ChatView.tsx</files>
  <action>
Create src/components/chat/ChatView.tsx as the self-contained chat feature component. Named export `ChatView`. Props interface:
```typescript
interface ChatViewProps {
  isPluginMode: boolean;
}
```

ChatView calls `useChat()` internally — it does NOT receive messages/input/send as props from App.tsx. This is the single location where useChat is called.

The component renders:
1. **Message list area**: A scrollable container (flex-1, overflow-y-auto) with a ref to the messages container area. Use AnimatePresence from 'motion/react' wrapping a list of `<motion.div>` elements (initial/animate/exit from App.tsx message animation). Each item renders `<MessageBubble message={msg} />`. Add a `ref={messagesEndRef}` sentinel div at the bottom.

2. **Empty state**: When messages.length === 0, show the empty state UI from App.tsx with the Bot icon, title ("Indian Tax Assistant"), subtitle, and quick queries grid. The quickQueries array should be a local constant in this file — do NOT thread it as a prop. Quick query buttons call `send()` after setting input... wait — they call a combined action. In App.tsx these buttons set input and then call handleSend. In ChatView, define a helper: `const sendQuickQuery = (query: string) => { /* call useChat's setInput then send, or use a dedicated sendWithText approach */ }`. The cleanest approach: expose a `sendText(text: string)` from useChat that directly sends a provided string without relying on input state. If useChat does not expose sendText, add it: it sets the message directly and calls sendChatMessage without going through setInput. Check the actual App.tsx quick query handler and replicate the behavior exactly.

3. **Loading indicator**: Show the three-dot typing indicator (from App.tsx lines 479-496) when isLoading === true, rendered after the last message. This can be inline in ChatView or extracted — keep it inline here for simplicity.

4. **ChatInput**: At the bottom, render `<ChatInput input={input} isLoading={isLoading} onInputChange={setInput} onSend={send} />`

Preserve all Tailwind styling from App.tsx for the chat area container, message list, and empty state layout. Import from 'motion/react' (not framer-motion). Import cn from '../../lib/utils', useChat from '../../hooks/useChat', MessageBubble from './MessageBubble', ChatInput from './ChatInput'.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirm ChatView calls useChat() internally. Run: grep -n "useChat" src/components/chat/ChatView.tsx — should show the import and the call. Confirm ChatView does NOT accept messages or isLoading as props (those come from the hook).</manual>
  </verify>
  <done>ChatView.tsx compiles with zero TypeScript errors, calls useChat() internally, renders message list with animations, empty state, loading indicator, and ChatInput — matching the visual structure of App.tsx's chat section.</done>
</task>

</tasks>

<verification>
Run `cd D:/tax-assistant && npx tsc --noEmit` — zero errors.
Run `grep -rn "from 'framer-motion'" src/` — zero results (all imports must use 'motion/react').
Run `grep -n "useChat()" src/components/chat/ChatView.tsx` — exactly one result.
Run `grep -rn "function cn" src/` — still exactly one result (src/lib/utils.ts).
</verification>

<success_criteria>
- All four chat components exist under src/components/chat/ with named exports
- ChartRenderer renders bar and pie charts from JSON, returns null on parse failure
- ChatInput fires onSend on Enter-without-Shift and when send button clicked
- MessageBubble splits content on json-chart blocks, renders ChartRenderer or Markdown for each part
- ChatView owns useChat state, shows message list with animations, empty state, and input
- Zero TypeScript errors across all four files
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-architecture/02-03-SUMMARY.md` following the summary template.
</output>
