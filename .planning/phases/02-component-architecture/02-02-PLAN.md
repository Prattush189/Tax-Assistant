---
phase: 02-component-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/hooks/useTheme.ts
  - src/hooks/usePluginMode.ts
  - src/hooks/useChat.ts
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "useTheme returns isDarkMode boolean and toggleTheme function; side-effect syncs to localStorage and document.documentElement.classList"
    - "usePluginMode returns isPluginMode boolean derived from URLSearchParams without re-computing on re-renders"
    - "useChat returns messages, input, setInput, isLoading, messagesEndRef, send, and clearChat; calling send() streams AI response into messages state"
  artifacts:
    - path: "src/hooks/useTheme.ts"
      provides: "isDarkMode state + localStorage sync effect"
      exports: ["useTheme"]
    - path: "src/hooks/usePluginMode.ts"
      provides: "isPluginMode from URL search params"
      exports: ["usePluginMode"]
    - path: "src/hooks/useChat.ts"
      provides: "Full chat state orchestration"
      exports: ["useChat"]
  key_links:
    - from: "src/hooks/useChat.ts"
      to: "src/services/api.ts"
      via: "sendChatMessage(input, messages, onChunk, onError)"
      pattern: "sendChatMessage"
    - from: "src/hooks/useChat.ts"
      to: "src/types/index.ts"
      via: "Message[] type"
      pattern: "Message\\[\\]"
---

<objective>
Extract the three custom hooks from App.tsx into standalone files: useTheme (dark mode + localStorage), usePluginMode (URL param detection), and useChat (full message streaming orchestration).

Purpose: These hooks encapsulate all business logic. After this plan, components become pure renderers — they accept props and call hooks, containing no state or async logic of their own.
Output: src/hooks/useTheme.ts, src/hooks/usePluginMode.ts, src/hooks/useChat.ts
</objective>

<execution_context>
@C:/Users/Prattush/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Prattush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-architecture/02-RESEARCH.md
@src/App.tsx
@.planning/phases/02-component-architecture/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useTheme and usePluginMode hooks</name>
  <files>src/hooks/useTheme.ts, src/hooks/usePluginMode.ts</files>
  <action>
Create src/hooks/useTheme.ts by extracting the isDarkMode state and its useEffect from App.tsx (the block that syncs to localStorage and document.documentElement.classList). The hook must:

- Initialize isDarkMode with a lazy initializer function that reads localStorage.getItem('theme') === 'dark', OR falls back to window.matchMedia('(prefers-color-scheme: dark)').matches if no stored value — guard with `typeof window !== 'undefined'` for SSR safety
- useEffect on [isDarkMode]: add/remove 'dark' class on document.documentElement, write 'dark'/'light' to localStorage
- Return: `{ isDarkMode, toggleTheme: () => setIsDarkMode(prev => !prev) }`

Create src/hooks/usePluginMode.ts by extracting the isPluginMode useMemo from App.tsx:

- useMemo with empty dependency array: reads `new URLSearchParams(window.location.search).get('plugin') === 'true'`
- Guard with `typeof window !== 'undefined'`
- Return: `{ isPluginMode }`

No imports from other project files needed for these two hooks. Import only from 'react'.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm useTheme returns { isDarkMode: boolean, toggleTheme: () => void }. Confirm usePluginMode returns { isPluginMode: boolean }.</manual>
  </verify>
  <done>Both hooks exist, compile cleanly, and return the expected shapes matching the research-documented API.</done>
</task>

<task type="auto">
  <name>Task 2: useChat hook</name>
  <files>src/hooks/useChat.ts</files>
  <action>
Create src/hooks/useChat.ts by extracting the chat state and handleSend logic from App.tsx. This is the most complex extraction — follow these constraints precisely:

**State to include:**
- `messages` useState<Message[]>([])
- `input` useState<string>('')
- `isLoading` useState<boolean>(false)
- `messagesEndRef` useRef<HTMLDivElement>(null)
- Drop `chatContainerRef` — it is dead code in App.tsx (assigned but never read)

**scrollToBottom effect:**
- useEffect with [messages] dependency: `messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' })`

**send function (extracted from handleSend in App.tsx):**
- Guard: if isLoading or input.trim() === '' return
- Create userMessage: `{ role: 'user', content: input.trim(), timestamp: new Date() }`
- setMessages(prev => [...prev, userMessage]) — MUST use functional updater form
- setInput('')
- setIsLoading(true)
- Add empty model message: `setMessages(prev => [...prev, { role: 'model', content: '', timestamp: new Date() }])`
- Call `sendChatMessage(userMessage.content, messages, onChunk, onError)` from api.ts where:
  - onChunk: `(text) => setMessages(prev => { const updated = [...prev]; updated[updated.length - 1] = { ...updated[updated.length - 1], content: updated[updated.length - 1].content + text }; return updated; })` — MUST use functional updater form, do not simplify to direct array assignment
  - onError: `(msg) => setMessages(prev => { const updated = [...prev]; updated[updated.length - 1] = { ...updated[updated.length - 1], content: msg }; return updated; })`
- finally: setIsLoading(false)

**clearChat function:**
- setMessages([])
- setInput('')

**Return shape:**
`{ messages, input, setInput, isLoading, messagesEndRef, send, clearChat }`

Import Message from '../types'. Import sendChatMessage from '../services/api'. Import useState, useRef, useEffect from 'react'.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirm useChat returns all 7 values in the return shape. Run: grep -n "setMessages(prev" src/hooks/useChat.ts — should return at least 3 lines confirming all setMessages calls use functional updater form.</manual>
  </verify>
  <done>useChat.ts compiles cleanly, imports from api.ts and types, returns { messages, input, setInput, isLoading, messagesEndRef, send, clearChat }, and all setMessages calls use the functional updater form.</done>
</task>

</tasks>

<verification>
Run `cd D:/tax-assistant && npx tsc --noEmit` — zero errors.
Run `grep -rn "setMessages(prev" src/hooks/useChat.ts` — at least 3 matches confirming functional updater pattern throughout.
Run `grep -n "chatContainerRef" src/hooks/useChat.ts` — zero results (dead code excluded).
</verification>

<success_criteria>
- All three hooks exist and compile with zero TypeScript errors
- useTheme: initializes from localStorage, syncs dark class + localStorage in effect
- usePluginMode: reads URLSearchParams.get('plugin') via useMemo
- useChat: all setMessages calls use functional updater form; integrates with sendChatMessage from api.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-architecture/02-02-SUMMARY.md` following the summary template.
</output>
