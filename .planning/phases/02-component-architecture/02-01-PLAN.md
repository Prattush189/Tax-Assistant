---
phase: 02-component-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/utils.ts
  - src/services/api.ts
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "All shared TypeScript interfaces (Message, UploadResponse, HistoryItem) exist in one importable module"
    - "The cn() utility is available to all components from a single location"
    - "All /api/* fetch calls are encapsulated in api.ts — no raw fetch calls in hooks or components"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Message, UploadResponse, HistoryItem type definitions"
      exports: ["Message", "UploadResponse", "HistoryItem"]
    - path: "src/lib/utils.ts"
      provides: "cn() class composition utility"
      exports: ["cn"]
    - path: "src/services/api.ts"
      provides: "sendChatMessage(), uploadFile() — all /api/* fetch calls"
      exports: ["sendChatMessage", "uploadFile"]
  key_links:
    - from: "src/services/api.ts"
      to: "/api/chat"
      via: "fetch POST with SSE stream reading"
      pattern: "fetch.*api/chat"
    - from: "src/services/api.ts"
      to: "/api/upload"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/upload"
---

<objective>
Create the three foundation modules that every other file in Phase 2 will import from: shared types, the cn() utility, and the api service layer.

Purpose: Establishing these before any hook or component is written prevents forward references, import cycles, and the cn() duplication anti-pattern. No behavior changes — only code organization.
Output: src/types/index.ts, src/lib/utils.ts, src/services/api.ts
</objective>

<execution_context>
@C:/Users/Prattush/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Prattush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-architecture/02-RESEARCH.md
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared types module and cn() utility</name>
  <files>src/types/index.ts, src/lib/utils.ts</files>
  <action>
Create src/types/index.ts with exactly these three exports (derived from App.tsx data shapes):

```typescript
export interface Message {
  role: 'user' | 'model';
  content: string;
  timestamp: Date;
}

export interface UploadResponse {
  success: boolean;
  filename: string;
  mimeType: string;
  sizeBytes: number;
  summary: string;
}

// Shape expected by /api/chat server route
export interface HistoryItem {
  role: string;
  parts: Array<{ text: string }>;
}
```

Create src/lib/utils.ts with a single cn() export:

```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Both clsx and tailwind-merge are already in package.json. Do not add any other exports or logic to these files.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm src/types/index.ts exports Message, UploadResponse, HistoryItem. Confirm src/lib/utils.ts exports cn. No TypeScript errors in these two files.</manual>
  </verify>
  <done>src/types/index.ts and src/lib/utils.ts exist, compile cleanly, and export the correct named symbols.</done>
</task>

<task type="auto">
  <name>Task 2: API service module</name>
  <files>src/services/api.ts</files>
  <action>
Create src/services/api.ts by extracting the fetch logic from App.tsx. The SSE stream parsing logic MUST be copied verbatim — do not simplify or restructure it. Specifically:

1. The `buffer` accumulator pattern with `decoder.decode(value, { stream: true })` is required. The `stream: true` flag and the `buffer = lines.pop() ?? ''` line must both be preserved exactly.
2. The functional updater forms for setMessages are NOT in api.ts — api.ts only does fetch + SSE parsing, calling onChunk/onError callbacks.

The file should contain exactly these two exported async functions:

**sendChatMessage** — POST /api/chat with SSE stream:
- Signature: `export async function sendChatMessage(message: string, history: Message[], onChunk: (text: string) => void, onError: (msg: string) => void): Promise<void>`
- Converts history from Message[] to HistoryItem[] format (role + parts[{text}]) before sending
- Handles non-streaming error responses (response.ok === false): parse JSON, call onError with errData.error or fallback string
- Reads SSE stream: buffer + split on '\n' + pop remainder; for each line starting with 'data: ', parse JSON payload; call onChunk on parsed.text, call onError on parsed.error, return on '[DONE]'
- Catches malformed JSON chunks silently (empty catch block)

**uploadFile** — POST /api/upload with FormData:
- Signature: `export async function uploadFile(file: File): Promise<UploadResponse>`
- Appends file to FormData under field name 'file' (server expects this exact field name)
- Throws Error with data.error or 'Upload failed.' on non-ok response

Import Message and UploadResponse from '../types'. Import HistoryItem from '../types' for the conversion. Do not import from any other project file.
  </action>
  <verify>
    <automated>cd D:/tax-assistant && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm src/services/api.ts exports sendChatMessage and uploadFile. Run: grep -n "buffer +=" src/services/api.ts — should return exactly one line showing the SSE buffer accumulation.</manual>
  </verify>
  <done>src/services/api.ts compiles with no TypeScript errors, exports sendChatMessage and uploadFile, and contains the SSE stream parsing logic moved verbatim from App.tsx.</done>
</task>

</tasks>

<verification>
Run `cd D:/tax-assistant && npx tsc --noEmit` — zero errors.
Run `grep -rn "function cn" src/` — exactly one result (src/lib/utils.ts).
Run `grep -rn "fetch(" src/` — results only in src/services/api.ts (App.tsx still has its own fetch calls at this point — that is expected; they will be removed in Plan 04).
</verification>

<success_criteria>
- src/types/index.ts exports Message, UploadResponse, HistoryItem
- src/lib/utils.ts exports cn() using clsx + tailwind-merge
- src/services/api.ts exports sendChatMessage and uploadFile with correct signatures
- TypeScript compiles with zero errors across all three new files
- SSE buffer accumulation logic present verbatim in api.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-architecture/02-01-SUMMARY.md` following the summary template.
</output>
